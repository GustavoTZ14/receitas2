---
import Layout from '../layouts/Layout.astro';
import HeroImageReceita from '../components/receitas/HeroImageReceitas.astro';
import MainContent from '../components/receitas/MainContent.astro';
import FooterContent from '../components/receitas/FooterContent.astro';
import Receitas from '../data/receitas.json';

export function getStaticPaths(){
  return Receitas.map(({
    slug, 
    titulo, 
    imagem, 
    descricao, 
    tempoPreparo, 
    nivel, 
    categoria, 
    rendimento,
    ingredientes,
    preparo
  }) =>{
    return{
      params: {slug},
      props: {
        titulo, 
        imagem,
        descricao,
        tempoPreparo,
        nivel,
        categoria,
        rendimento,
        ingredientes,
        preparo
      }
    }
  })
}

const {
  titulo, 
  imagem, 
  descricao, 
  tempoPreparo,
  nivel,
  categoria,
  rendimento,
  ingredientes,
  preparo
} = Astro.props;
---
<Layout title={titulo}>
  <HeroImageReceita 
    imagem={imagem} 
    titulo={titulo} 
    descricao={descricao} 
    tempoPreparo={tempoPreparo}
    nivel={nivel}
    categoria={categoria}
    rendimento={rendimento}
  />
  <MainContent ingredientes={ingredientes} preparo={preparo}/>
  <FooterContent/>
</Layout>
